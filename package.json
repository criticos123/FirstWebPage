{
  "name": "Meldev_Website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "react:dev": "yarn --cwd project1 start",
    "keystone:dev": " PORT=3001 yarn --cwd keystone dev",
    "dev": "npm-run-all --parallel react:dev keystone:dev"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
