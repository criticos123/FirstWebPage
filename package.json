{
  "name": "Meldev_Website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "next:dev": "yarn --cwd meldev dev",
    "keystone:dev": "yarn --cwd keystone dev",
    "keystone:start": "PORT=$PORT cd keystone && npm install && npm run build && npm run start",
    "dev": "npm-run-all --parallel next:dev keystone:dev"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
