{
  "name": "Meldev_Website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "next:dev": "yarn --cwd meldev dev",
    "sanity:dev": "yarn --cwd sanity start",
    "dev": "npm-run-all --parallel next:dev sanity:dev"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
