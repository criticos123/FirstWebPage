import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4169E1",
    },
    secondary: {
      main: "#A32CC4",
    },
  },
});

export default theme;
